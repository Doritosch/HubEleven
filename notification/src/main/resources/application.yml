spring:
  application:
    name: notification-service

  config:
    import: "optional:configserver:"

  cloud:
    config:
      discovery:
        enabled: true
        service-id: config

  profiles:
    active: eureka, swagger
